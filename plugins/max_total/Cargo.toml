[package]
name = "max-total"
version = "0.1.0"
edition = "2021"

[dependencies]
wit-bindgen-rt = { version = "0.28.0", features = ["bitflags"] }
shared = { path = "../shared" }

[lib]
crate-type = ["cdylib"]

[package.metadata.component]
package = "example:max-total"
